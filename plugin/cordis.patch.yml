# dsh-desktop-pet bundle patch: inserts its plugin row into a dsh profile roster.
# 也可以不用 bundle：直接把下面这行追加到 profile 的 cordis.patch.yml。
- insert:
    - id: desktop-pet
      name: '@linxin666/dsh-desktop-pet'
